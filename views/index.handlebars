  <nav>
    <div class="nav-wrapper amber darken-2">
      <a class="brand-logo">Doggo</a>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
          <li><a href="/auth/logout">Logout</a></li>
          <li><a href="/auth/login">Login</a></li>
          <li><a href="/">Home</a></li>
      </ul>
    </div>
  </nav>
<div class="image-container1">
  <h4 class="jawn">Find a dog breed that suits your personality!</h4>
</div>
<button class="google-oauth"><a href="/auth/login">Login</a></button>

<div class="container">
  <div class="row">
    <div class="col s8">
      <form class="breedSurvey">
        <div class="row">
          <div class="input-field col s6">
            <input placeholder="John Smith" id="name" type="text" class="validate">
            <label for="name">Name</label>
          </div>
          <div class="input-field col s6">
            <input id="zipcode" type="text" class="validate">
            <label for="zipcode">Zipcode</label>
          </div>
        </div>
        {{#each questionsAndChoices}}
        <div class="row">
          <div class="input-field col s12">
            <select id="{{this.id}}">
              <option value="" disabled selected>Choose your option</option>
              {{#each this.Choices}}
              <option value="{{this.id}}">{{this.choice}}</option>
              {{/each}}
            </select>
            <label class="question" data="{{this.id}}">{{this.question}}</label>
          </div>
        </div>
        {{/each}}

        <button id="submitSurvey" class="btn waves-effect waves-light" type="submit" name="action">
          Submit
        </button>
      </form>
    </div>
  </div>
</div>

{{!-- javascript tags --}}
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.0/jquery.min.js"></script>
<script src="/js/survey.js"></script>