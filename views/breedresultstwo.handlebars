<div class="container">
  <div class="row">
  {{#each returnedArray}}
    <div class="col s12 m12">
      <img class="circle" src={{image}}>
    <div class="card horizontal">
      <div class="card-stacked">
        <span class="card-title">{{breed_name}}</span>
        <div class="card-content">
          <ul>
            {{#each Attributes}}
            <li>{{attribute}} : <img class="stars" data-score="{{score}}" src=""></li>
            {{/each}}
          </ul>
        </div>
        <div class="card-action">
         <button class="btn waves-effect waves-light" type="submit" name="action" id="findAdoption"
            data-breed="{{breed_name}}">Find</button>
        </div>
      </div>
    </div>
        </div>
  {{/each}}
</div>
</div>

<!--jquery-->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.0/jquery.min.js"></script>
{{!-- javascript tags --}}
<script src="/js/breedResults.js"></script>